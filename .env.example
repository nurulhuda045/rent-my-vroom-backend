# Environment
NODE_ENV=development

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/rentmyvroom?schema=public

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Cloudflare R2 (Object Storage)
R2_ACCESS_KEY=your_r2_access_key
R2_SECRET_KEY=your_r2_secret_key
R2_BUCKET_NAME=your_bucket_name
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://your-public-url.r2.dev

# Email Configuration (Optional - for notifications)
EMAIL_FROM=noreply@rentmyvroom.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Application
PORT=3000
FRONTEND_URL=http://localhost:3000

# WhatsApp Business Cloud API Configuration
# Get these from: https://developers.facebook.com/apps/
# 1. Create a Facebook Developer account
# 2. Create a new app and add WhatsApp product
# 3. Get your access token and phone number ID from the WhatsApp API Setup page
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_API_VERSION=v21.0

# OTP Configuration
# OTP_EXPIRY_MINUTES: How long the OTP is valid (default: 5 minutes)
# OTP_MAX_ATTEMPTS: Maximum verification attempts per OTP (default: 3)
# OTP_RESEND_COOLDOWN_SECONDS: Cooldown between OTP resend requests (default: 30 seconds)
OTP_EXPIRY_MINUTES=5
OTP_MAX_ATTEMPTS=3
OTP_RESEND_COOLDOWN_SECONDS=30
